Create branch and merge to origin

    -Create branch: $ git checkout -b [name_of_your_new_branch]
    
    -Change working branch : $ git checkout [name_of_your_new_branch]
    
    -Push the branch on github : $ git push origin [name_of_your_new_branch]